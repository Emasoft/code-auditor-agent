# fixture for linux_kernel_tree — top-level linux-style Makefile using
# in-tree `obj-y` (not `obj-m`) so this is distinct from a single
# out-of-tree module.

obj-y += arch/x86/kernel/
obj-y += drivers/foo/
obj-y += kernel/
