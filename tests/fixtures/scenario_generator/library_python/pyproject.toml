[project]
name = "mylib"
version = "0.1.0"
description = "A tiny library fixture for the library_python discoverer tests."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["mylib"]
