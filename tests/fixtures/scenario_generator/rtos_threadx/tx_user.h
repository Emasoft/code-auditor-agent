/* fixture for rtos_threadx — tx_user.h is part of the primary
 * content trigger for the threadx fingerprint (the fingerprint
 * requires "tx_api.h" or "tx_user.h" substrings in some .h file).
 */
#ifndef TX_USER_H
#define TX_USER_H

#include "tx_api.h"

#define TX_TIMER_TICKS_PER_SECOND   100

#endif /* TX_USER_H */
