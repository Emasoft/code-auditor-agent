# fixture for data_pipeline_airflow
# Minimal Airflow configuration used to trigger the
# data_pipeline_airflow fingerprint. Real-world configs are far larger.

[core]
dags_folder = /opt/airflow/dags
executor = SequentialExecutor
load_examples = False

[scheduler]
catchup_by_default = False
