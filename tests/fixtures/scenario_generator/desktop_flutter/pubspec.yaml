# fixture for desktop_flutter (targets windows, linux, macos)
name: demo_desktop_flutter
description: Demo Flutter desktop app fixture
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_acrylic: ^1.1.0
  window_manager: ^0.3.0
  desktop_window: ^0.4.0

flutter_supported_platforms:
  - windows
  - linux
  - macos
