# fixture for mobile_flutter
name: demo_flutter_app
description: Demo Flutter mobile app fixture
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
