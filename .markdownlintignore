# Dev folders contain draft/in-progress docs — not published
docs_dev/
scripts_dev/
samples_dev/
examples_dev/
tests_dev/
builds_dev/
reports_dev/
reports/

# CHANGELOG.md is auto-generated by git-cliff from commit messages.
# Commit-message emphasis style (mixed _x_ / *x*) isn't worth fixing
# on each release — and publish.py's Phase 3.5 regenerates it just
# before Phase 4's pre-push lint, so any per-commit fixes get rolled
# back at release time. Excluding the file is the right call.
CHANGELOG.md
